<?xml version="1.0" encoding="utf-8"?>
<!--
    Layout: activity_lista_tareas.xml
    Descripción:
    Pantalla que muestra todas las tareas y exámenes registrados por el usuario.
    Utiliza un RecyclerView para listar las tareas, un mensaje cuando no hay tareas pendientes
    y un botón flotante (FAB) para agregar nuevas actividades.
    Incluye un menú lateral (Navigation Drawer) para navegar entre las secciones de la aplicación.
-->

<androidx.drawerlayout.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ListaTareasActivity">

    <!-- Contenedor principal -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:background="@color/fondo_pantalla">

        <!-- Barra superior con botón de menú y título -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="16dp"
            android:gravity="center_vertical">

            <!-- Botón para abrir el menú lateral -->
            <ImageButton
                android:id="@+id/btnMenu"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:src="@android:drawable/ic_menu_sort_by_size"
                app:tint="@color/icono_menu" />

            <!-- Título centrado -->
            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Mis Tareas y Exámenes"
                android:textSize="20sp"
                android:textStyle="bold"
                android:textColor="@color/texto_principal"
                android:gravity="center"
                android:layout_marginEnd="32dp"/>
        </LinearLayout>

        <!-- Contenedor del contenido principal -->
        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <!-- RecyclerView que muestra la lista de tareas -->
            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/rvListaTareas"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:paddingTop="8dp"
                android:paddingBottom="80dp"
                android:clipToPadding="false"/>

            <!-- Mensaje que aparece si no hay tareas -->
            <TextView
                android:id="@+id/tvSinTareas"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:text="¡Todo listo! No hay tareas pendientes"
                android:textSize="16sp"
                android:textColor="@color/texto_secundario"
                android:visibility="gone"/>

            <!-- Botón flotante para agregar nueva tarea -->
            <com.google.android.material.floatingactionbutton.FloatingActionButton
                android:id="@+id/fabAgregarTarea"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="bottom|end"
                android:layout_margin="24dp"
                android:src="@android:drawable/ic_input_add"
                app:backgroundTint="#4CAF50"
                app:tint="#FFFFFF"
                android:contentDescription="Agregar Tarea" />
        </FrameLayout>
    </LinearLayout>

    <!-- Menú lateral (Navigation Drawer) -->
    <com.google.android.material.navigation.NavigationView
        android:id="@+id/nav_view"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:fitsSystemWindows="true"
        app:headerLayout="@layout/nav_header"
        app:menu="@menu/menu_lateral" />
</androidx.drawerlayout.widget.DrawerLayout>
